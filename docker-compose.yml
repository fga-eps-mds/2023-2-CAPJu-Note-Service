version: "3.3"

services:
  note:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: note
    command:
      [
        "npm",
        "run",
        "start",
      ]
    restart: always
    volumes:
      - ./:/app
    ports:
      - 8083:8083
    networks:
      - capju
    env_file:
      - .env

networks:
  capju:
    external: true
