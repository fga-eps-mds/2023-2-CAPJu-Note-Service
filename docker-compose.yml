version: "3.3"

services:
  note:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: note
    command:
      [
        "npm",
        "run",
        "start",
      ]
    restart: always
    volumes:
      - ./:/app
    ports:
      - 8083:8083
    networks:
      - 2023-2-capju-config_capju
    env_file:
      - .env

networks:
  2023-2-capju-config_capju:
    external: true
